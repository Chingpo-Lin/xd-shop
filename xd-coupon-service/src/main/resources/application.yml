
server:
  port: 9002

spring:
  application:
    name: xd-coupon-service
  cloud:
    nacos:
      discovery:
        server-addr: 120.24.94.91:8848

  # database config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.24.94.91:3306/xd_coupon?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=US/Pacific
    username: root
    password: xdclass.net168
  # redis config
  redis:
    host: 120.24.94.91
    port: 6379
    password: ljb112233

# config mybatis plus print sql
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# config log level,ERROR/WARN/INFO/DEBUG,default is show when higher than INFO level
logging:
  level:
    root: INFO

# seata config
seata:
  tx-service-group: ${spring.application.name}-group
  service:
    grouplist:
      xdbob: 127.0.0.1:8091
    vgroup-mapping:
      xd-coupon-service-group: xdbob